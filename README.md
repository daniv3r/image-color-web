# image-color-web

Support, privacy policy, and terms of use site for **PicNum: Paint by Number**, built with Jekyll for GitHub Pages.

TODO: Set `app_store_url` in `_config.yml` to the final App Store product URL when it is available. Until then, the home page shows a non-interactive prelaunch message instead of a broken link.

## Local preview

```
bundle exec jekyll serve
```

Then open http://localhost:4000.
